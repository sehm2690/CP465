Udatabase
    tables
        users
            userid 
            password 
            first name
            last name 
            email
        
        portfolio 
            userid
            see website
        watchlist
            userid
            see website
            
        wallet
            userid
            total amount //i.e $1000
            current amount //i.e $10



To do list
        1. login and create account (Wilson)

        2. portfolio (R/P/L)

        3. watchlist (R/P/L)

        - clean up

        4. news (Wilson )

        5. beautify


later FLEX:
    more watch list 
    Watchlist vardump unknown tickers 
    colour code %change and change


Monday - 3PM
Wednesday - VARIES
Friday - 3PM


DUE APRIL 12TH 
    3 Weeks


ALTER TABLE watchlist
ADD price FLOAT
ADD change FLOAT
ADD percent_change FLOAT


CREATE DATABASE stonkstrader;
USE stonkstrader;

CREATE TABLE portfolio(
	PID INT NOT NULL AUTO_INCREMENT, 
    UID INT NOT NULL,
    symbol VARCHAR(6) NOT NULL,
    name VARCHAR(255) NOT NULL, 
    qty INT NOT NULL,
    price FLOAT NOT NULL,
    PRIMARY KEY (PID),
    FOREIGN KEY(UID) REFERENCES users(UID)
);


CREATE TABLE user(
	UID INT NOT NULL AUTO_INCREMENT, 
    firstname VARCHAR(255) NOT NULL, 
    lastname VARCHAR(255) NOT NULL, 
    email VARCHAR(255) NOT NULL,
    password VARCHAR(50) NOT NULL, 
    cash FLOAT NOT NULL DEFAULT 100000,
    PRIMARY KEY (UID)
);


CREATE TABLE watchlist(
	WID INT NOT NULL AUTO_INCREMENT, 
    UID INT NOT NULL,
    type INT,
    symbol VARCHAR(6) NOT NULL,
    name VARCHAR(255) NOT NULL, 
    description VARCHAR(1000),    
    FOREIGN KEY(UID) REFERENCES users(UID)
);


CREATE TABLE transactions(
	TID INT NOT NULL AUTO_INCREMENT, 
    UID INT NOT NULL,
    transact VARCHAR(5) NOT NULL, 
    symbol VARCHAR(10) NOT NULL,
    qty INT NOT NULL,
    amount FLOAT NOT NULL,
    FOREIGN KEY(UID) REFERENCES users(UID)
);




